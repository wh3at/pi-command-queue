# コマンドキュー

pi に入力した内容を順番に処理するための操作領域。

## Language

**キューモード**:
Enter で確定した入力を即時送信せず、キューに追加する状態。送信した最後の項目の処理が終わると自動的に解除され、送信前にキューが空になっても解除されない。

**キュー項目**:
キューモード中に確定された、一回分の入力。通常メッセージ、組み込みコマンド、スキル・プロンプトテンプレートの呼び出し、シェル実行入力をそのまま保持し、ほかの拡張が登録したコマンドは含まない。

**未送信キュー**:
まだ pi に送られていないキュー項目を、追加された順に並べたもの。実行中の項目は含まない。

**実行中項目**:
未送信キューから取り出して pi に送った後、処理の完了を待っている項目。

**一時停止**:
後続の未送信項目がある状態で実行中項目が失敗・中断されたか、選択画面がキャンセルされ、送信を止めている状態。続行か破棄を選ぶまで新しい項目は受け付けない。
