# キューへの取り込みはメイン入力欄で行う

pi の通常の `input` フックは `/new` などの組み込みコマンドが処理された後に呼ばれるため、組み込みコマンドと通常入力をともに FIFO に積む要件を満たせない。キューモードでは拡張のカスタムエディタで送信を捕捉し、取り出した項目は pi 本来の送信ハンドラへ渡す。エディタを置き換える別の拡張と競合し得るが、組み込みコマンドをモデル向けの文字列に変えないために選んだ。
