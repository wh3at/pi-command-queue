# セッション切替をまたぐキューは pi プロセス内に保持する

pi は `/new` などのセッション切替時に拡張ファクトリを再実行するため、ファクトリ内だけの状態では後続のキューが消える。キュー本体を pi プロセス内の共有状態として保持し、新しい拡張 API と TUI をセッション開始時に結び直す。ディスク保存は再起動時の意図しない再送につながるため採用せず、`/reload` と終了時には共有状態を破棄する。
