# pi 本体を変更せず拡張だけで提供する

pi 0.87.1 の拡張 API には、組み込みコマンドの失敗や、通常入力の送信前エラーを確実に受け取る手段がない。完了・失敗イベントを pi 本体に追加する案も検討したが、拡張だけで提供することを優先した。検知できるエージェントの失敗・中断やシェル実行の異常終了では後続を一時停止する一方、検知不能な失敗の後に次項目が送られる可能性を残す。ほかの拡張が登録したコマンドは完了も観測できないためキュー対象外とし、ON 中の入力は送信せず入力欄に残す。この制約と誤送信リスクを利用者向け説明に明記する。
